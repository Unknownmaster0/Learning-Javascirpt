// statement -> which doesn't produce any value.
// expression -> which give some value.

// example -> 4 + 5-6 = expression.
// const me = 'sagar singh' -> not a expression.

// all operators produces value, thus they are expression.
// example -> 
const age = 19;
const me = age >= 18 ? "wine 🍷" : 'water 💧';
console.log(me);